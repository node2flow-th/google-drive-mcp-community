services:
  google-drive-mcp-community:
    build: .
    container_name: google-drive-mcp-community
    restart: unless-stopped
    ports:
      - "127.0.0.1:3031:3000"
    mem_limit: 512m
    mem_reservation: 256m
